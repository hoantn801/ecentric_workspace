# PM System Audit — báo cáo sáng (đêm 03→04/08/2026)

**Người làm:** Clo (autonomous overnight). **Nguyên tắc:** KHÔNG deploy, KHÔNG chạm production. Mọi fix nằm dạng **code trên branch** để bạn review + deploy. Nothing changed on team.ecentric.vn while you slept.

---

## 1. TL;DR
- Đã **đọc & audit toàn bộ PM** (frontend `pm_app.html` ~2.5k dòng + toàn bộ `pm/api/*.py` + `permissions.py` + hooks) bằng 2 audit agent song song → **48 findings**.
- Đã **tự fix 19 cái an toàn** (cosmetic + robustness + vài lỗ hổng permission rõ ràng) → gom thành **branch `fix/pm-audit`** (3 commit, 5 file, diff nhỏ). Verified: `py_compile` ✅ + `node --check` ✅.
- **Chưa deploy gì.** Bạn review branch → merge → deploy khi thấy ổn.
- Phần **cần bạn quyết định** (có tradeoff / đổi hành vi / refactor lớn) → liệt kê ở §4 kèm đề xuất.

---

## 2. Branch trạng thái

| Branch | Nội dung | Trạng thái |
|---|---|---|
| `fix/pm-ui-tweaks` | #1 icon giờ + underline, #2 bỏ timer, #3 recurring deadlock+Sinh ngay | Đã push, đang chờ PR/merge. **#3 backend đã deploy tối qua.** |
| `fix/pm-audit` | **MỚI** — 19 fix từ audit, stack trên `fix/pm-ui-tweaks` | Nằm local trong repo, **chưa push**. |

> `fix/pm-audit` xây bằng `commit-tree` nên **không đụng working tree** của bạn (không cần lo branch đang đứng). Push khi muốn: `git push -u origin fix/pm-audit`.

Commits:
```
7b94414 fix(pm/ui): search-icon leak trên 3 form nữa + responsive + wording
a9ba2f8 fix(pm/backend): robustness - per-item isolation scans, log lỗi bị nuốt
66c9291 fix(pm/security): vá lỗ hổng permission  [REVIEW BEFORE DEPLOY]
```

---

## 3. Đã fix (trên `fix/pm-audit`) — an toàn, chỉ chờ deploy

### 3a. Frontend cosmetic (commit 7b94414)
1. **Icon kính lúp leak — 3 form nữa.** Hoá ra lỗi này **hệ thống** chứ không phải lẻ: base `.pm-input` gắn sẵn icon search + `padding-left:30px`, nên mọi chỗ tái dùng mà quên override đều dính. Đã fix 3 chỗ còn leak: **form tạo việc định kỳ** (ô tên/người nhận/mô tả/ngày/giờ), **ô ngày trong quick-add nhiệm vụ con**, và **ô lý do + ngày trong Từ chối/Đổi lịch của Assignment**.
2. **Responsive:** `dc-mini` (dashboard mini-cards, đang khoá 5 cột) + `pm-props` (task detail 2 cột) giờ có breakpoint mobile → không bị bóp méo dưới ~760/560px.
3. **Wording:** nút topbar **"New Task" → "Tạo nhiệm vụ"** (đồng bộ, cả UI đang tiếng Việt).
4. **Error toast** giờ ở lại **5 giây** (trước 2.4s — thông báo lỗi biến mất trước khi đọc kịp).

### 3b. Backend robustness (commit a9ba2f8)
5. **`pm_overdue_scan` / `pm_due_soon_scan`** (job nhắc quá hạn/sắp đến hạn chạy hằng ngày): giờ **try/except + commit theo từng task** — 1 task lỗi không còn làm hỏng cả lượt quét (giống cách `run_due` đã làm).
6. **Recurrence notify** lỗi giờ **log** thay vì `pass` im lặng (dễ truy vết).
7. **Dashboard `wstart`** bọc `str()` — chống footgun so sánh ngày kiểu string vs date sau này.

### 3c. Backend security — ⚠️ REVIEW kỹ trước deploy (commit 66c9291)
8. **`recurrence.get_for_task`** trước chỉ `require_pm_access()` → **bất kỳ PM user nào cũng đọc được lịch định kỳ của task bất kỳ**. Đã thêm `can_view_task` (giống các endpoint anh em).
9. **`can_view_task` / `is_task_assignee`** trước match `_assign` bằng **substring** → email là chuỗi con của email khác bị lọt quyền (vd `an@ec.vn` khớp task giao cho `man@ec.vn` → xem + đổi trạng thái được). Đã đổi sang **so khớp đúng phần tử JSON list**; query LIKE cũng quote lại (`%"user"%`).
> Đây là **siết quyền** (tighten). Rủi ro thấp nhưng vì đổi hành vi access nên mình để riêng commit + gắn cờ REVIEW. Test lại với 1 user thường trước khi deploy.

**Verify:** `py_compile` 4 file .py ✅ · `node --check` pm_app.html ✅ · diff chỉ đụng 5 file, không CRLF churn.

---

## 4. CẦN BẠN QUYẾT ĐỊNH (mình KHÔNG tự làm — có tradeoff)

Sắp theo mức ưu tiên. Mỗi mục có đề xuất của mình.

### 🔴 Ưu tiên cao
- **D1 — Root-cause icon leak (đề xuất: LÀM).** Fix ở §3a chỉ vá 3 chỗ hiện tại; gốc rễ là base `.pm-input` mang sẵn icon. Đề xuất **đảo lại**: `.pm-input` thành input trơn, icon search chuyển vào modifier `.pm-input.pm-search`, chỉ gắn cho 2 ô search thật. Rồi xoá 12 chỗ `background-image:none` rải rác. → Từ đó **không bao giờ tái phát**. Blast radius rộng (nhiều call site) nên cần bạn OK.
- **D2 — Deadlock ở các path tương tác (đề xuất: LÀM, giống recurrence).** `tasks.create`/`update` và `assignment` cũng ghi nested-set của Task y như chỗ đã dính deadlock 1213. Hiện **chưa có retry** → dưới tải cao, tạo/sửa task đồng thời có thể 500. Đề xuất bọc retry dùng lại helper đã viết cho `run_due`. Cần quyết ranh giới retry cho request tương tác.
- **D3 — Dashboard tải TOÀN BỘ task vào Python** (`control_center` leader-view `limit_page_length=0` + ~12 vòng quét). 10× dữ liệu là nặng. Đề xuất chuyển sang **đếm bằng SQL `GROUP BY`/`db.count`** + cap danh sách. Là redesign hiệu năng nên cần lịch riêng.

### 🟡 Ưu tiên vừa
- **D4 — Batch endpoint tin frontend.** `checklist.counts` & `labels.labels_for_tasks_api` nhận list task_names từ client, chỉ `require_pm_access`, không `can_view_task` → lộ tiến độ checklist/label của task ngoài scope. Code đang ghi chú "non-sensitive". Quyết: siết `can_view_task` hay chấp nhận.
- **D5 — Recurrence tháng bị trôi ngày.** Rule mùng 31 → Feb 28 rồi kẹt 28 mãi (trôi khỏi cuối tháng). Quyết: neo theo day-of-month gốc hay giữ hành vi hiện tại.
- **D6 — `errOf` / `errBox`.** Toast đôi khi hiện raw `_server_messages` JSON; `errBox` báo lỗi generic + không có nút "Thử lại". Đề xuất parse `_server_messages` + thêm nút retry. (Nhỏ, làm được nếu OK.)
- **D7 — Xác nhận khi "Huỷ quy tắc" định kỳ.** Xoá project/task có confirm modal, huỷ recurrence thì không. Đề xuất thêm confirm cho nhất quán.
- **D8 — Nút "Cài đặt" (gear) topbar** không có handler → bấm không làm gì. Wire hoặc ẩn.
- **D9 — 3 nút composer bình luận** (đính kèm/link/mention) đang `disabled` vĩnh viễn ("sắp có"). Ẩn cho gọn hay giữ.

### 🟢 Thấp / dọn dẹp
- **D10** — a11y: hàng bảng/`dc-link`/alert là `div/tr` có onclick nhưng không keyboard-accessible (thêm `role/tabindex/keydown`).
- **D11** — `projects.get` đếm theo `status` native, chỗ khác theo `workflow_state` → số liệu lệch. Thống nhất.
- **D12** — Định nghĩa "terminal" 2 nơi khác nhau (dashboard `_FINISHED` vs `permissions.is_task_terminal`).
- **D13** — Gộp helper parse `_assign` (lặp ở ≥5 module) thành `permissions.assignees_of()` (cũng là điểm sửa duy nhất cho D-security §3c).
- **D14** — `tasks.assign` / `timesheet.log` / `projects.create` chưa validate assignee/ngày chặt như `tasks.create` → có thể 500 thay vì báo lỗi thân thiện. (Mình có sẵn diff, làm nhanh nếu OK.)
- **D15** — Mock/preview code (`file://`) còn ship trong prod; hardcoded hex thay vì CSS var; nút chuông mở Frappe Desk tab mới. Dọn dần.

*(Danh sách đầy đủ 48 findings + line refs mình giữ trong log audit, cần thì bạn nói mình xuất chi tiết từng cái.)*

---

## 5. Cách review & deploy (khi bạn dậy)

1. Xem branch: `git log --oneline fix/pm-ui-tweaks..fix/pm-audit` và `git diff fix/pm-ui-tweaks fix/pm-audit`.
2. Push khi ưng: `git push -u origin fix/pm-audit` → mở PR vào `main` (stack sau `fix/pm-ui-tweaks`).
3. Deploy giống #3: `bench restart` (nạp .py) + đẩy `pm_app.html` lên Web Page (`deploy_pm_app.ps1`). Nhớ **Ctrl+Shift+R** để qua prerender cache.
4. **Riêng commit security `66c9291`**: test 1 user thường (không phải leader) xem vẫn xem/đổi được task của mình + không xem được task ngoài scope, trước khi yên tâm.

## 6. Đề xuất next (nếu bạn muốn mình làm tiếp)
Ưu tiên mình khuyến nghị: **D1 (root-cause icon)** → **D2 (deadlock path tương tác)** → **D14 (validate nhanh)** → **D6 (error UX)**. Ba cái đầu tăng độ bền hệ thống rõ rệt; mình làm được dạng code-review-ready như branch này.

---

## 7. UPDATE — đã xử lý cả 15 decision (bạn bảo "làm hết")

Toàn bộ nằm trên branch **`fix/pm-audit`** (giờ 6 commit, 11 file, verified `py_compile` + `node --check`, **chưa deploy**).

**✅ Đã code (11/15):**
| # | Nội dung | Commit |
|---|---|---|
| D1 | Đảo base `.pm-input` → icon vào `.pm-search` (chống tái phát icon leak vĩnh viễn) | `1733e8d` |
| D2 | `pm/deadlock.py` retry + bọc `tasks.create/update` (hết 500 do deadlock nested-set) | `71cdcea` |
| D4 | Scope `checklist.counts` + `labels_for_tasks_api` về task caller được xem (helper `visible_task_subset`) | `0f49af2` |
| D5 | Recurrence tháng neo theo ngày gốc (hết trôi khỏi 31) | `0f49af2` |
| D6 | `errOf` parse `_server_messages`; `errBox` hiện lỗi thật + nút "Thử lại" | `1733e8d` |
| D7 | `confirm()` trước khi Huỷ quy tắc lặp (2 chỗ) | `1733e8d` |
| D8 | Bỏ nút Cài đặt (gear) chết | `1733e8d` |
| D9 | Ẩn 3 nút composer disabled vĩnh viễn | `1733e8d` |
| D11 | `projects.get` đếm theo `workflow_state` (đồng bộ) | `0f49af2` |
| D13 | Helper chung `assignees_of` (điểm sửa duy nhất cho substring bug) | `71cdcea` |
| D14 | Validate assignee (`tasks.assign`) + ngày (`timesheet.log`) | `71cdcea`/`0f49af2` |

**⏸ Cố ý CHƯA code (4/15) — vì làm mù đêm nay rủi ro, cần bạn xác nhận số liệu/hành vi trước:**
- **D3 — Dashboard redesign (đếm bằng SQL GROUP BY).** Đổi cách tính số liệu dashboard → phải so số trước/sau trên data thật. Mình không muốn commit mù cái đổi con số hiển thị. *Approach sẵn, làm khi bạn OK ngồi so số ~15').*
- **D10 — Keyboard a11y (role/tabindex/keydown cho row & link).** Đụng nhiều render function, dễ làm hỏng tương tác chuột nếu làm ẩu. Cần 1 pass riêng có test.
- **D12 — Thống nhất định nghĩa "terminal".** Dashboard dùng `workflow_state`, guard dùng cả `status` native → gộp sẽ **đổi con số** trên dashboard. Cần xác nhận semantics mong muốn.
- **D15 — Dọn mock/preview + hardcoded hex.** Header code ghi chú team dùng `file://` preview → **không dám xoá mù**. Hex→token là churn thuần, để dọn dần.

→ 4 cái này mình để **recommendation + approach** ở đây; bạn gật cái nào mình làm cái đó (có runtime check).

## Branch cuối: `fix/pm-audit` (6 commit)
```
0f49af2 D4,D5,D11,D14  backend consistency/scope
71cdcea D2,D13,D14     deadlock retry + shared helper
1733e8d D1,D6-D9       icon root-cause + error UX + confirms
66c9291 security        get_for_task + _assign exact-match  [REVIEW]
a9ba2f8 robustness      per-item scans + log
7b94414 ui sweep        3 form icon leak + responsive + wording
```
Push: `git push -u origin fix/pm-audit` → PR sau `fix/pm-ui-tweaks`. Deploy: `bench restart` + đẩy `pm_app.html`. Test 1 user thường cho 2 commit permission (`66c9291` + D4).

*Không có gì chạm production đêm nay. Chúc buổi sáng tốt lành 🌤️*
